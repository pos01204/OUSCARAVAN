# SolAPI를 통한 카카오 알림톡/네이버 메시지 발송 가이드

## 📋 개요

**SolAPI**는 n8n에 공식 노드로 등록되어 있어 카카오 알림톡, 네이버 메시지, SMS를 간편하게 발송할 수 있는 서비스입니다.

**장점:**
- ✅ n8n 공식 노드 지원 (설정 간편)
- ✅ 카카오 알림톡 발송 가능
- ✅ 네이버 메시지 발송 가능
- ✅ SMS 발송 가능
- ✅ 가입 조건 상대적으로 간단
- ✅ 통합 관리 가능

---

## 🔍 SolAPI 서비스 개요

### 지원 메시지 유형

1. **카카오 알림톡**
   - 친구 추가 없이 전화번호로 발송
   - 템플릿 기반 발송
   - 링크 버튼 지원

2. **네이버 메시지**
   - 네이버 예약 연동 가능
   - 템플릿 기반 발송

3. **SMS**
   - 일반 문자 메시지
   - 모든 휴대폰에서 수신

---

## 1단계: SolAPI 서비스 신청

### 1-1. SolAPI 가입

1. [SolAPI](https://solapi.com/) 접속
2. **"회원가입"** 클릭
3. 사업자 정보 입력:
   - 회사명: `오우스카라반`
   - 사업자 등록번호: `887-08-01964`
   - 대표자명: 입력
4. 가입 완료

### 1-2. 서비스 활성화

1. SolAPI 콘솔 로그인
2. **"서비스 신청"** 메뉴 확인
3. 필요한 서비스 활성화:
   - 카카오 알림톡
   - 네이버 메시지 (선택)
   - SMS (선택)

### 1-3. API 인증 정보 발급

1. SolAPI 콘솔 → **"API 설정"** 또는 **"인증 정보"**
2. **API Key** 및 **API Secret** 발급
3. 복사 및 보관

---

## 2단계: 카카오 알림톡 템플릿 등록

### 2-1. 카카오 비즈니스 채널 확인

SolAPI를 통해 알림톡을 발송하려면 카카오 비즈니스 채널이 필요합니다.

1. [카카오 비즈니스](https://business.kakao.com/) 접속
2. 채널 생성 및 인증 완료 확인
3. 채널 ID 확인

### 2-2. SolAPI에서 템플릿 등록

1. SolAPI 콘솔 → **"알림톡"** → **"템플릿 관리"**
2. **"템플릿 등록"** 클릭
3. 카카오 비즈니스 채널 선택
4. 템플릿 정보 입력:

**템플릿 이름:**
```
예약 완료 안내
```

**템플릿 내용:**
```
#{guest_name}님, OUSCARAVAN 예약이 완료되었습니다!

📅 예약번호: #{reservation_number}
📅 체크인: #{checkin_date} 15:00
📅 체크아웃: #{checkout_date} 11:00
🏠 객실: #{room_type}
💰 결제금액: #{amount}원

컨시어지 서비스를 이용하시려면 아래 버튼을 클릭하세요.
```

**버튼 추가:**
- **버튼 유형**: 링크 버튼
- **버튼 이름**: `컨시어지 서비스 이용하기`
- **링크**: `#{service_link}`

5. **저장** 클릭
6. 카카오 비즈니스에서 템플릿 승인 (1-2일)

### 2-3. 템플릿 코드 확인

템플릿 승인 완료 후:

1. SolAPI 콘솔 → **"알림톡"** → **"템플릿 관리"**
2. 승인된 템플릿 클릭
3. **"템플릿 코드"** 복사
4. n8n 환경 변수에 저장

---

## 3단계: n8n SolAPI 노드 설정

### 3-1. SolAPI 노드 추가

1. n8n 워크플로우 편집
2. **"+" 버튼** 클릭
3. 검색창에 **"SolAPI"** 입력
4. **"SolAPI"** 노드 선택

### 3-2. SolAPI 인증 설정

**Credential 설정:**

1. **"Credential"** 드롭다운에서 **"Create New Credential"** 선택
2. **Credential Name**: `SolAPI OUSCARAVAN`
3. **API Key**: SolAPI에서 발급받은 API Key 입력
4. **API Secret**: SolAPI에서 발급받은 API Secret 입력
5. **Save** 클릭

### 3-3. SolAPI 노드 설정

**Operation**: `Send AlimTalk` (카카오 알림톡 발송)

**Parameters:**

| 필드 | 값 | 설명 |
|------|-----|------|
| `to` | `{{ $json.phone }}` | 수신자 전화번호 |
| `templateId` | `{{ $env.SOLAPI_ALIMTALK_TEMPLATE_ID }}` | 템플릿 ID |
| `variables` | 아래 JSON 참조 | 템플릿 변수 |

**Variables (템플릿 변수):**
```json
{
  "#{guest_name}": "{{ $json.guest }}",
  "#{reservation_number}": "{{ $json.reservationNumber }}",
  "#{checkin_date}": "{{ $json.checkin }}",
  "#{checkout_date}": "{{ $json.checkout }}",
  "#{room_type}": "{{ $json.room }}",
  "#{amount}": "{{ $json.amount }}",
  "#{service_link}": "{{ $json.link }}"
}
```

**또는 Code 노드에서 변수 생성:**

**Code 노드 추가 (템플릿 변수 생성):**
```javascript
const variables = {
  "#{guest_name}": $input.item.json.guest,
  "#{reservation_number}": $input.item.json.reservationNumber,
  "#{checkin_date}": $input.item.json.checkin,
  "#{checkout_date}": $input.item.json.checkout,
  "#{room_type}": $input.item.json.room,
  "#{amount}": $input.item.json.amount,
  "#{service_link}": $input.item.json.link
};

return {
  ...$input.item.json,
  solapi_variables: variables
};
```

**SolAPI 노드에서:**
- `variables`: `{{ $json.solapi_variables }}`

---

## 4단계: 전화번호 포맷 변환

### 4-1. Code 노드 추가

**"고유 링크 생성"** 노드와 **"SolAPI"** 노드 사이에 추가:

```javascript
// 전화번호에서 하이픈 제거 및 포맷 정리
let phone = $input.item.json.phone || '';

// 하이픈, 공백, 괄호 제거
phone = phone.replace(/[-\s()]/g, '');

// 국제번호 제거 (010으로 시작하는 경우)
if (phone.startsWith('010')) {
  phone = phone.substring(0, 11); // 01012345678 형식
} else if (phone.startsWith('82')) {
  // 82로 시작하는 경우 (국제번호)
  phone = '0' + phone.substring(2);
  if (phone.length > 11) {
    phone = phone.substring(0, 11);
  }
}

// 전화번호 유효성 검사
if (!phone || phone.length < 10) {
  throw new Error(`유효하지 않은 전화번호: ${phone}`);
}

return {
  ...$input.item.json,
  phone: phone,
  phone_formatted: phone
};
```

---

## 5단계: 환경 변수 설정

### 5-1. n8n 환경 변수 추가

1. n8n 상단 메뉴 → **Settings** (⚙️ 아이콘)
2. **Environment Variables** 클릭
3. 다음 변수 추가:

| Name | Value | 설명 |
|------|-------|------|
| `SOLAPI_ALIMTALK_TEMPLATE_ID` | 템플릿 ID | SolAPI에서 승인받은 템플릿 ID |
| `WEB_APP_URL` | 웹 앱 URL | 기존 유지 |

**참고:** SolAPI API Key와 Secret은 Credential로 관리하므로 환경 변수에 추가하지 않아도 됩니다.

---

## 6단계: 워크플로우 구조

### 최종 워크플로우 구조

```
Gmail Trigger
  ↓
IF (예약 확정/취소 구분)
  ↓
Code (이메일 파싱)
  ↓
Set (데이터 정리)
  ↓
Code (고유 링크 생성)
  ↓
Code (전화번호 포맷 변환)
  ↓
Code (템플릿 변수 생성) ← 선택사항
  ↓
SolAPI (카카오 알림톡 발송) ← SolAPI 노드 사용
```

---

## 7단계: SolAPI 노드 상세 설정

### 7-1. Operation 선택

**SolAPI 노드에서:**

1. **Operation** 드롭다운에서 **"Send AlimTalk"** 선택
2. 또는 **"Send Message"** (일반 메시지)

### 7-2. 필수 필드 입력

**Send AlimTalk 설정:**

| 필드 | 값 | 설명 |
|------|-----|------|
| `to` | `{{ $json.phone }}` | 수신자 전화번호 (하이픈 제거) |
| `templateId` | `{{ $env.SOLAPI_ALIMTALK_TEMPLATE_ID }}` | 템플릿 ID |
| `variables` | `{{ $json.solapi_variables }}` | 템플릿 변수 (JSON 객체) |

**또는 직접 입력:**

**Variables (JSON):**
```json
{
  "#{guest_name}": "{{ $json.guest }}",
  "#{reservation_number}": "{{ $json.reservationNumber }}",
  "#{checkin_date}": "{{ $json.checkin }}",
  "#{checkout_date}": "{{ $json.checkout }}",
  "#{room_type}": "{{ $json.room }}",
  "#{amount}": "{{ $json.amount }}",
  "#{service_link}": "{{ $json.link }}"
}
```

### 7-3. 추가 옵션

**선택 필드:**
- `reserveTime`: 예약 발송 시간 (선택사항)
- `scheduleCode`: 스케줄 코드 (선택사항)

---

## 8단계: 테스트

### 8-1. 단일 테스트

1. **"전화번호 포맷 변환"** 노드에서 **"Execute Node"** 클릭
2. 전화번호 포맷 확인
3. **"SolAPI"** 노드에서 **"Execute Node"** 클릭
4. SolAPI 응답 확인

### 8-2. 전체 플로우 테스트

1. 테스트용 네이버 예약 완료 이메일 발송
2. n8n 워크플로우 자동 실행 확인
3. 각 노드 실행 결과 확인
4. 알림톡 수신 확인

---

## 📋 체크리스트

### SolAPI 서비스 신청
- [ ] SolAPI 가입
- [ ] 서비스 활성화 (카카오 알림톡)
- [ ] API Key 및 API Secret 발급

### 카카오 알림톡 템플릿
- [ ] 카카오 비즈니스 채널 생성 및 인증
- [ ] SolAPI에서 템플릿 등록
- [ ] 카카오 비즈니스에서 템플릿 승인
- [ ] 템플릿 ID 확인

### n8n 설정
- [ ] SolAPI 노드 추가
- [ ] SolAPI Credential 설정
- [ ] 전화번호 포맷 변환 노드 추가
- [ ] 템플릿 변수 생성 노드 추가 (선택)
- [ ] 환경 변수 추가 (템플릿 ID)
- [ ] 테스트 실행

---

## 🆘 문제 해결

### 문제 1: SolAPI 노드를 찾을 수 없음

**원인:** n8n에 SolAPI 노드가 설치되지 않음

**해결:**
1. n8n에서 **"Community Nodes"** 확인
2. SolAPI 노드 설치
3. 또는 n8n 버전 확인 (최신 버전 권장)

### 문제 2: "401 Unauthorized" 오류

**원인:** API Key 또는 Secret 오류

**해결:**
1. SolAPI Credential 설정 확인
2. API Key 및 Secret 재확인
3. SolAPI 콘솔에서 API Key 상태 확인

### 문제 3: "템플릿을 찾을 수 없음" 오류

**원인:** 템플릿 ID 오류 또는 템플릿 미승인

**해결:**
1. 템플릿 ID 확인
2. 템플릿 승인 상태 확인
3. 카카오 비즈니스에서 템플릿 승인 완료 확인

### 문제 4: "전화번호 형식 오류" 오류

**원인:** 전화번호 포맷 오류

**해결:**
1. 전화번호 포맷 변환 노드 확인
2. 하이픈 제거 확인
3. 전화번호 유효성 검사 확인

---

## 💡 SolAPI 추가 기능

### 네이버 메시지 발송

SolAPI는 네이버 메시지도 발송할 수 있습니다:

1. **Operation**: `Send NaverTalk` 선택
2. 네이버 메시지 템플릿 등록
3. 동일한 방식으로 발송

### SMS 발송

일반 SMS도 발송 가능:

1. **Operation**: `Send SMS` 선택
2. 메시지 내용 직접 입력
3. 전화번호로 발송

---

## 🎯 최종 워크플로우 구조

```
[네이버 예약 완료]
  ↓
[예약 완료 이메일 발송]
  ↓
[n8n Gmail Trigger]
  ↓
[IF: 예약 확정/취소 구분]
  ↓
[Code: 이메일 파싱]
  ↓
[Set: 데이터 정리]
  ↓
[Code: 고유 링크 생성]
  ↓
[Code: 전화번호 포맷 변환]
  ↓
[Code: 템플릿 변수 생성] (선택)
  ↓
[SolAPI: 카카오 알림톡 발송]
  ↓
[알림톡 수신]
  ↓
[게스트가 채널 친구 추가] (유도)
  ↓
[향후 마케팅 친구톡 발송]
```

---

## 📊 SolAPI vs 직접 API 비교

| 항목 | SolAPI | 직접 API |
|------|--------|----------|
| **n8n 노드** | 공식 노드 지원 ✅ | HTTP Request 노드 사용 |
| **설정 복잡도** | 간단 ✅ | 복잡 ❌ |
| **가입 조건** | 상대적으로 간단 ✅ | 까다로움 ❌ |
| **템플릿 관리** | SolAPI 콘솔에서 관리 ✅ | 카카오 비즈니스에서 관리 |
| **비용** | 건당 20-30원 | 건당 20-30원 |
| **다중 서비스** | 알림톡/SMS/네이버 통합 ✅ | 각각 별도 설정 |

---

## 🎯 최종 권장사항

### SolAPI 사용 (권장)

**이유:**
1. ✅ n8n 공식 노드 지원으로 설정 간편
2. ✅ 가입 조건 상대적으로 간단
3. ✅ 카카오 알림톡, 네이버 메시지, SMS 통합 관리
4. ✅ 템플릿 관리 용이

**다음 단계:**
1. SolAPI 가입 및 서비스 활성화
2. 카카오 알림톡 템플릿 등록 및 승인
3. n8n SolAPI 노드 설정
4. 테스트 실행

---

**문서 버전**: 1.0  
**최종 업데이트**: 2024-01-15
