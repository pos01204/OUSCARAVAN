# 웹뷰 호환 인증 시스템 구현 작업 로그

> **작업 시작일**: 2026-01-19  
> **작업 완료일**: 2026-01-19  
> **작업 상태**: ✅ 완료  
> **관련 문서**: [AUTH_WEBVIEW_IMPROVEMENT_PLAN.md](./AUTH_WEBVIEW_IMPROVEMENT_PLAN.md)

---

## 📋 작업 상세 요청서

### 목표
외부 앱(카카오톡, 네이버 등) 웹뷰에서도 정상적으로 로그인이 가능한 하이브리드 인증 시스템 구현

### 핵심 변경사항
1. **토큰 저장**: httpOnly 쿠키 → localStorage (클라이언트)
2. **토큰 전송**: Authorization 헤더 우선, 쿠키 폴백
3. **로그인 방식**: 서버 액션 → 클라이언트 API 호출

### 작업 범위

| 영역 | 파일 | 작업 유형 | 상태 |
|------|------|----------|------|
| 백엔드 | `railway-backend/package.json` | 수정 | ✅ 완료 |
| 백엔드 | `railway-backend/src/app.ts` | 수정 | ✅ 완료 |
| 백엔드 | `railway-backend/src/middleware/auth.middleware.ts` | 수정 | ✅ 완료 |
| 백엔드 | `railway-backend/src/routes/admin.routes.ts` | 수정 | ✅ 완료 |
| 프론트엔드 | `lib/auth-client.ts` | 신규 생성 | ✅ 완료 |
| 프론트엔드 | `lib/api.ts` | 수정 | ✅ 완료 |
| 프론트엔드 | `app/(auth)/login/page.tsx` | 수정 | ✅ 완료 |
| 프론트엔드 | `components/admin/AuthGuard.tsx` | 신규 생성 | ✅ 완료 |
| 프론트엔드 | `components/admin/LogoutButton.tsx` | 신규 생성 | ✅ 완료 |
| 프론트엔드 | `app/admin/layout.tsx` | 수정 | ✅ 완료 |

---

## 📝 작업 진행 로그

### Phase 1: 백엔드 수정 ✅

#### 1.1 cookie-parser 패키지 설치
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `railway-backend/package.json`
- **변경 내용**: 
  ```json
  // dependencies에 추가
  "cookie-parser": "^1.4.6"
  
  // devDependencies에 추가
  "@types/cookie-parser": "^1.4.6"
  ```

#### 1.2 app.ts 쿠키 파서 미들웨어 추가
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `railway-backend/src/app.ts`
- **변경 내용**: 
  ```typescript
  import cookieParser from 'cookie-parser';
  
  // 미들웨어 추가
  app.use(cookieParser());
  ```

#### 1.3 auth.middleware.ts 하이브리드 인증
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `railway-backend/src/middleware/auth.middleware.ts`
- **변경 내용**: 
  - `authenticate()`: Authorization 헤더 우선, 쿠키 폴백
  - `authenticateApiKey()`: n8n API Key 인증
  - `authenticateOrApiKey()`: 하이브리드 인증 (사용자/API Key)
  - `optionalAuthenticate()`: 선택적 인증

#### 1.4 admin.routes.ts 인증 활성화
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `railway-backend/src/routes/admin.routes.ts`
- **변경 내용**: 
  ```typescript
  import { authenticateOrApiKey } from '../middleware/auth.middleware';
  
  // 인증 미들웨어 활성화
  router.use(authenticateOrApiKey);
  ```

---

### Phase 2: 프론트엔드 수정 ✅

#### 2.1 auth-client.ts 생성
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `lib/auth-client.ts`
- **주요 함수**:
  - `saveToken(token, expiresIn)`: 토큰 저장
  - `getToken()`: 토큰 조회
  - `clearToken()`: 토큰 삭제
  - `isAuthenticated()`: 로그인 상태 확인
  - `getAuthHeader()`: Authorization 헤더 생성
  - `getTokenRemainingTime()`: 토큰 만료까지 남은 시간
  - `shouldRefreshToken()`: 토큰 갱신 필요 여부

#### 2.2 api.ts 인증 헤더 로직 추가
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `lib/api.ts`
- **변경 내용**: 
  - `authenticatedFetch()`: 인증 헤더 포함 fetch 래퍼
  - `adminApi()`: 하이브리드 인증 적용
  - 401 에러 시 자동 토큰 클리어 및 리다이렉트

#### 2.3 login/page.tsx 클라이언트 컴포넌트로 교체
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `app/(auth)/login/page.tsx`
- **변경 내용**: 
  - 서버 액션 → 클라이언트 API 호출
  - localStorage 토큰 저장
  - 에러 메시지 표시 개선
  - 비밀번호 표시/숨기기 기능
  - Suspense로 useSearchParams 래핑

#### 2.4 AuthGuard.tsx 생성
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `components/admin/AuthGuard.tsx`
- **기능**:
  - 인증 상태 확인
  - 미인증 시 로그인 페이지 리다이렉트
  - 로딩 상태 표시

#### 2.5 LogoutButton.tsx 생성
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `components/admin/LogoutButton.tsx`
- **기능**:
  - 토큰 삭제
  - 로그인 페이지로 이동

#### 2.6 admin/layout.tsx AuthGuard 적용
- **상태**: ✅ 완료
- **완료 시간**: 2026-01-19
- **파일**: `app/admin/layout.tsx`
- **변경 내용**: 
  - AuthGuard로 children 래핑
  - LogoutButton 추가

---

## ✅ 완료된 작업 요약

| 작업 | 완료 시간 | 비고 |
|------|----------|------|
| 백엔드: cookie-parser 설치 | 2026-01-19 | package.json 수정 |
| 백엔드: 쿠키 파서 미들웨어 | 2026-01-19 | app.ts 수정 |
| 백엔드: 하이브리드 인증 미들웨어 | 2026-01-19 | auth.middleware.ts 전면 수정 |
| 백엔드: 인증 미들웨어 활성화 | 2026-01-19 | admin.routes.ts 수정 |
| 프론트엔드: auth-client.ts | 2026-01-19 | 신규 생성 |
| 프론트엔드: api.ts 인증 로직 | 2026-01-19 | authenticatedFetch 추가 |
| 프론트엔드: 로그인 페이지 | 2026-01-19 | 클라이언트 컴포넌트로 전환 |
| 프론트엔드: AuthGuard | 2026-01-19 | 신규 생성 |
| 프론트엔드: LogoutButton | 2026-01-19 | 신규 생성 |
| 프론트엔드: admin/layout.tsx | 2026-01-19 | AuthGuard, LogoutButton 적용 |

---

## 🔧 배포 전 필수 작업

### Railway 환경 변수 설정
```bash
# 필수 환경 변수
JWT_SECRET=<강력한-시크릿-키-32자-이상>
N8N_API_KEY=<n8n-자동화용-API-키>
```

### 백엔드 패키지 설치
```bash
cd railway-backend
npm install
```

### 테스트 체크리스트
- [ ] 일반 브라우저에서 로그인 테스트
- [ ] 카카오톡 웹뷰에서 로그인 테스트
- [ ] 네이버 앱 웹뷰에서 로그인 테스트
- [ ] 로그아웃 후 재로그인 테스트
- [ ] 세션 만료 후 자동 리다이렉트 테스트
- [ ] n8n API Key 인증 테스트

---

## 📁 변경된 파일 목록

### 백엔드 (railway-backend/)
```
railway-backend/
├── package.json                              # cookie-parser 추가
└── src/
    ├── app.ts                                # cookieParser 미들웨어 추가
    ├── middleware/
    │   └── auth.middleware.ts                # 하이브리드 인증 구현
    └── routes/
        └── admin.routes.ts                   # 인증 미들웨어 활성화
```

### 프론트엔드
```
lib/
├── auth-client.ts                            # 신규: 클라이언트 토큰 관리
└── api.ts                                    # 수정: authenticatedFetch 추가

app/
├── (auth)/
│   └── login/
│       └── page.tsx                          # 수정: 클라이언트 컴포넌트
└── admin/
    └── layout.tsx                            # 수정: AuthGuard 적용

components/
└── admin/
    ├── AuthGuard.tsx                         # 신규: 인증 상태 확인
    └── LogoutButton.tsx                      # 신규: 로그아웃 버튼
```

---

## 🔄 변경 이력

| 날짜 | 변경 내용 | 변경자 |
|------|----------|--------|
| 2026-01-19 | 작업 로그 문서 생성 | AI |
| 2026-01-19 | 백엔드 작업 완료 (Phase 1) | AI |
| 2026-01-19 | 프론트엔드 작업 완료 (Phase 2) | AI |
| 2026-01-19 | 작업 완료, 문서 최종 업데이트 | AI |

---

## 📌 참고 사항

### 인증 흐름 요약
```
1. 로그인 요청
   └── POST /api/auth/login (id, password)
   
2. 토큰 발급
   └── JWT 토큰 반환 (7일 유효)
   
3. 토큰 저장
   └── localStorage → sessionStorage → cookie 폴백 저장 (웹뷰 호환)
   
4. API 호출
   └── Authorization: Bearer <token> 헤더로 전송
   
5. 인증 검증 (백엔드)
   ├── 1순위: Authorization 헤더
   └── 2순위: 쿠키 (폴백)
   
6. 로그아웃
   └── localStorage/sessionStorage/cookie 토큰 삭제 → 로그인 페이지 이동
```

---

## 🚨 운영 이슈 진단 및 후속 패치 (2026-01-19)

### 증상 1: 웹뷰에서 `인증 확인 중...` 화면이 진행되지 않음

- **원인**
  - `components/admin/AuthGuard.tsx`에서 미인증 분기 시 `router.replace()`만 호출하고 `setIsChecking(false)`가 호출되지 않아, 웹뷰에서 라우팅이 지연/실패하면 화면이 **무한 로딩 상태로 고정**될 수 있음.
  - 일부 웹뷰(iOS/Safari 계열, 앱 내 브라우저)에서 **localStorage 접근이 차단/예외**가 발생해 토큰 저장/조회가 실패할 수 있음.

- **해결**
  - `AuthGuard` 미인증 분기에서 `setIsChecking(false)`로 로딩을 종료하고, `window.location.replace()`를 **폴백 리다이렉트**로 추가.
  - `lib/auth-client.ts`에 **다중 저장소 폴백(localStorage → sessionStorage → cookie)** 구현:
    - 저장: localStorage/ sessionStorage 시도 + cookie 기록
    - 조회: localStorage → sessionStorage → cookie 순서
    - 삭제: 3곳 모두 정리

### 증상 2: Railway 로그에 `rooms_name_key` 중복(예: 1호 이미 존재) 에러 발생

- **원인**
  - 기존 `run-migrations.ts`의 rooms 마이그레이션이 `A1`→`1호`로 **rename**하는 방식이라, 이미 `1호`가 존재하는 환경에서는 `UNIQUE(name)` 충돌이 발생.
  - 충돌 시 마이그레이션이 부분 실패/스킵되며, 결과적으로 **rooms가 10개를 보장하지 못함**.

- **해결**
  - `railway-backend/src/migrations/run-migrations.ts`를 수정하여 rooms를 다음 원칙으로 **정규화**:
    - `reservations.assigned_room`은 `A1~B2` → `1호~10호`로 매핑
    - `rooms`는 `1호~10호`를 **upsert**로 보정
    - `rooms`에서 `1호~10호` 외 데이터는 **삭제**하여 항상 10개만 유지

### 보안 고려사항
- localStorage는 XSS 공격에 취약할 수 있음
- 프로덕션에서는 Content Security Policy (CSP) 설정 권장
- JWT_SECRET은 충분히 강력한 값 사용 (32자 이상)

---

*이 문서는 웹뷰 호환 인증 시스템 구현 작업의 전체 로그입니다.*
