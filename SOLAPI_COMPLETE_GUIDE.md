# SolAPI를 통한 카카오 알림톡/네이버 메시지 자동 발송 완전 가이드

## 📋 개요

**SolAPI**는 n8n에 공식 노드로 등록되어 있어 카카오 알림톡, 네이버 메시지, SMS를 간편하게 발송할 수 있는 서비스입니다.

**왜 SolAPI인가?**
- ✅ n8n 공식 노드 지원 (설정 간편)
- ✅ 가입 조건 상대적으로 간단
- ✅ 카카오 알림톡, 네이버 메시지, SMS 통합 관리
- ✅ 템플릿 관리 용이
- ✅ 비즈톡보다 가입 조건 덜 까다로움

---

## 🎯 전략 개요

### 목표
- 예약 완료 시 SolAPI를 통해 알림톡으로 즉시 자동 발송
- 카카오톡 채널 친구 추가 유도
- 향후 마케팅을 위한 친구톡 활용 기반 구축

### 전략 흐름
```
[네이버 예약 완료]
  ↓
[예약 완료 이메일 발송]
  ↓
[n8n Gmail Trigger]
  ↓
[IF: 예약 확정/취소 구분]
  ↓
[Code: 이메일 파싱]
  ↓
[Set: 데이터 정리]
  ↓
[Code: 고유 링크 생성]
  ↓
[Code: 전화번호 포맷 변환]
  ↓
[SolAPI: 카카오 알림톡 발송]
  ↓
[알림톡 수신]
  ↓
[게스트가 채널 친구 추가] (유도)
  ↓
[향후 마케팅 친구톡 발송]
```

---

## 1단계: SolAPI 서비스 신청

### 1-1. SolAPI 가입

1. [SolAPI](https://solapi.com/) 접속
2. **"회원가입"** 클릭
3. 사업자 정보 입력:
   - 회사명: `오우스카라반`
   - 사업자 등록번호: `887-08-01964`
   - 대표자명: 입력
   - 연락처: 전화번호 입력
4. 가입 완료

### 1-2. 서비스 활성화

1. SolAPI 콘솔 로그인
2. **"서비스 신청"** 또는 **"서비스 관리"** 메뉴 확인
3. 필요한 서비스 활성화:
   - **카카오 알림톡** (필수)
   - 네이버 메시지 (선택)
   - SMS (선택)

### 1-3. API 인증 정보 발급

1. SolAPI 콘솔 → **"API 설정"** 또는 **"인증 정보"** 또는 **"API 관리"**
2. **"API Key 발급"** 클릭
3. **API Key** 및 **API Secret** 발급
4. 복사 및 보관 (n8n Credential에 사용)

---

## 2단계: 카카오 비즈니스 채널 생성

### 2-1. 카카오 비즈니스 채널 생성

SolAPI를 통해 알림톡을 발송하려면 카카오 비즈니스 채널이 필요합니다.

1. [카카오 비즈니스](https://business.kakao.com/) 접속
2. 카카오 계정으로 로그인
3. **"비즈니스 시작하기"** 또는 **"채널 만들기"** 클릭
4. 채널 정보 입력:
   - 채널 이름: `OUSCARAVAN`
   - 채널 설명: `프리미엄 캠핑장/글램핑 컨시어지 서비스`
   - 대표 이미지: 채널 프로필 이미지 업로드
   - 연락처: 전화번호 입력
5. 사업자 정보 입력:
   - 회사명: `오우스카라반`
   - 사업자 등록번호: `887-08-01964`
   - 사업자등록증 업로드
6. **생성** 클릭

### 2-2. 채널 인증 완료

- 채널 생성 후 인증 절차 진행
- 인증 완료까지 1-2일 소요 가능

---

## 3단계: SolAPI에서 알림톡 템플릿 등록

### 3-1. SolAPI 템플릿 등록

1. SolAPI 콘솔 → **"알림톡"** → **"템플릿 관리"**
2. **"템플릿 등록"** 또는 **"템플릿 만들기"** 클릭
3. 카카오 비즈니스 채널 선택
4. 템플릿 정보 입력:

**템플릿 이름:**
```
예약 완료 안내
```

**템플릿 내용:**
```
#{guest_name}님, OUSCARAVAN 예약이 완료되었습니다!

📅 예약번호: #{reservation_number}
📅 체크인: #{checkin_date} 15:00
📅 체크아웃: #{checkout_date} 11:00
🏠 객실: #{room_type}
💰 결제금액: #{amount}원

컨시어지 서비스를 이용하시려면 아래 버튼을 클릭하세요.
```

**버튼 추가:**
- **버튼 유형**: 링크 버튼
- **버튼 이름**: `컨시어지 서비스 이용하기`
- **링크**: `#{service_link}` (템플릿 변수로 설정)

**템플릿 변수:**
- `#{guest_name}`: 게스트 이름
- `#{reservation_number}`: 예약번호
- `#{checkin_date}`: 체크인 날짜
- `#{checkout_date}`: 체크아웃 날짜
- `#{room_type}`: 객실 타입
- `#{amount}`: 결제금액
- `#{service_link}`: 컨시어지 서비스 링크

5. **저장** 클릭
6. 카카오 비즈니스에서 템플릿 승인 대기 (1-2일)

### 3-2. 템플릿 ID 확인

템플릿 승인 완료 후:

1. SolAPI 콘솔 → **"알림톡"** → **"템플릿 관리"**
2. 승인된 템플릿 클릭
3. **"템플릿 ID"** 또는 **"Template ID"** 복사
4. n8n 환경 변수에 저장

---

## 4단계: n8n SolAPI 노드 설정

### 4-1. SolAPI 노드 설치 확인

1. n8n 워크플로우 편집 화면
2. **"+" 버튼** 클릭
3. 검색창에 **"SolAPI"** 입력
4. **"SolAPI"** 노드가 있는지 확인

**노드가 없는 경우:**
1. n8n 상단 메뉴 → **Settings** → **Community Nodes**
2. **"SolAPI"** 검색
3. **"Install"** 클릭
4. 설치 완료 후 워크플로우 새로고침

### 4-2. SolAPI Credential 설정

1. n8n 워크플로우에서 **SolAPI 노드** 추가
2. **"Credential"** 드롭다운에서 **"Create New Credential"** 선택
3. **Credential Name**: `SolAPI OUSCARAVAN`
4. **API Key**: SolAPI에서 발급받은 API Key 입력
5. **API Secret**: SolAPI에서 발급받은 API Secret 입력
6. **Save** 클릭

### 4-3. SolAPI 노드 설정

**Operation**: `Send AlimTalk` (카카오 알림톡 발송)

**Parameters:**

| 필드 | 값 | 설명 |
|------|-----|------|
| `to` | `{{ $json.phone }}` | 수신자 전화번호 (하이픈 제거) |
| `templateId` | `{{ $env.SOLAPI_ALIMTALK_TEMPLATE_ID }}` | 템플릿 ID |
| `variables` | 아래 JSON 참조 | 템플릿 변수 |

**Variables (템플릿 변수):**

**Option A: 직접 입력**
```json
{
  "#{guest_name}": "{{ $json.guest }}",
  "#{reservation_number}": "{{ $json.reservationNumber }}",
  "#{checkin_date}": "{{ $json.checkin }}",
  "#{checkout_date}": "{{ $json.checkout }}",
  "#{room_type}": "{{ $json.room }}",
  "#{amount}": "{{ $json.amount }}",
  "#{service_link}": "{{ $json.link }}"
}
```

**Option B: Code 노드에서 생성 (권장)**

**Code 노드 추가 (템플릿 변수 생성):**
```javascript
const variables = {
  "#{guest_name}": $input.item.json.guest || '',
  "#{reservation_number}": $input.item.json.reservationNumber || '',
  "#{checkin_date}": $input.item.json.checkin || '',
  "#{checkout_date}": $input.item.json.checkout || '',
  "#{room_type}": $input.item.json.room || '',
  "#{amount}": $input.item.json.amount || '',
  "#{service_link}": $input.item.json.link || ''
};

return {
  ...$input.item.json,
  solapi_variables: variables
};
```

**SolAPI 노드에서:**
- `variables`: `{{ $json.solapi_variables }}`

---

## 5단계: 전화번호 포맷 변환 노드 추가

### 5-1. Code 노드 추가

**"고유 링크 생성"** 노드와 **"SolAPI"** 노드 사이에 추가:

**노드 이름**: `전화번호 포맷 변환`

**Mode**: `Run Once for All Items`

**Language**: `JavaScript`

**Code**:
```javascript
// 전화번호에서 하이픈 제거 및 포맷 정리
let phone = $input.item.json.phone || '';

// 하이픈, 공백, 괄호 제거
phone = phone.replace(/[-\s()]/g, '');

// 국제번호 제거 (010으로 시작하는 경우)
if (phone.startsWith('010')) {
  phone = phone.substring(0, 11); // 01012345678 형식
} else if (phone.startsWith('82')) {
  // 82로 시작하는 경우 (국제번호)
  phone = '0' + phone.substring(2);
  if (phone.length > 11) {
    phone = phone.substring(0, 11);
  }
}

// 전화번호 유효성 검사
if (!phone || phone.length < 10) {
  throw new Error(`유효하지 않은 전화번호: ${phone}`);
}

return {
  ...$input.item.json,
  phone: phone,
  phone_formatted: phone
};
```

**Save** 클릭

---

## 6단계: 환경 변수 설정

### 6-1. n8n 환경 변수 추가

1. n8n 상단 메뉴 → **Settings** (⚙️ 아이콘)
2. **Environment Variables** 클릭
3. 다음 변수 추가:

| Name | Value | 설명 |
|------|-------|------|
| `SOLAPI_ALIMTALK_TEMPLATE_ID` | 템플릿 ID | SolAPI에서 승인받은 템플릿 ID |
| `WEB_APP_URL` | 웹 앱 URL | 기존 유지 |

**참고:** SolAPI API Key와 Secret은 Credential로 관리하므로 환경 변수에 추가하지 않아도 됩니다.

---

## 7단계: 이메일 파싱 노드 수정 (전화번호 추출)

### 7-1. Code 노드 (이메일 파싱) 확인

전화번호를 추출하는 로직이 있는지 확인:

```javascript
// 기존 코드에 전화번호 추출 로직 추가
const phonePatterns = [
  /전화번호[:\s]*([0-9-()\s]+)/i,
  /연락처[:\s]*([0-9-()\s]+)/i,
  /휴대폰[:\s]*([0-9-()\s]+)/i,
  /010-?[0-9]{4}-?[0-9]{4}/,
  /01[0-9]-?[0-9]{3,4}-?[0-9]{4}/
];

let phone = '';
for (const pattern of phonePatterns) {
  const match = textBody.match(pattern);
  if (match) {
    phone = match[1] || match[0];
    break;
  }
}

// 전화번호 정리
phone = phone.replace(/[-\s()]/g, '').trim();

return {
  ...previousData,
  phone: phone
};
```

---

## 8단계: 테스트

### 8-1. 단일 노드 테스트

1. **"전화번호 포맷 변환"** 노드에서 **"Execute Node"** 클릭
2. 전화번호 포맷 확인
3. **"템플릿 변수 생성"** 노드에서 **"Execute Node"** 클릭 (있는 경우)
4. 변수 생성 확인
5. **"SolAPI"** 노드에서 **"Execute Node"** 클릭
6. SolAPI 응답 확인

### 8-2. 전체 플로우 테스트

1. 테스트용 네이버 예약 완료 이메일 발송
2. n8n 워크플로우 자동 실행 확인
3. 각 노드 실행 결과 확인
4. 알림톡 수신 확인

---

## 📋 체크리스트

### SolAPI 서비스 신청
- [ ] SolAPI 가입 완료
- [ ] API Key 및 Secret 발급
- [ ] 카카오 알림톡 서비스 활성화

### 카카오 비즈니스 채널
- [ ] 카카오 비즈니스 채널 생성
- [ ] 채널 인증 완료

### 템플릿 설정
- [ ] SolAPI에서 템플릿 등록
- [ ] 카카오 비즈니스에서 템플릿 승인
- [ ] 템플릿 ID 확인

### n8n 설정
- [ ] SolAPI 노드 설치 확인
- [ ] SolAPI Credential 생성
- [ ] 전화번호 포맷 변환 노드 추가
- [ ] 템플릿 변수 생성 노드 추가 (선택)
- [ ] SolAPI 노드 설정
- [ ] 환경 변수 추가 (템플릿 ID)
- [ ] 이메일 파싱 노드에 전화번호 추출 로직 추가

### 테스트
- [ ] 전화번호 포맷 변환 테스트
- [ ] 템플릿 변수 생성 테스트
- [ ] SolAPI 발송 테스트
- [ ] 전체 플로우 테스트

---

## 🆘 문제 해결

### 문제 1: SolAPI 노드를 찾을 수 없음

**원인:** n8n에 SolAPI 노드가 설치되지 않음

**해결:**
1. n8n 버전 확인 (최신 버전 권장)
2. Community Nodes에서 SolAPI 검색
3. 설치 후 워크플로우 새로고침
4. 또는 n8n 관리자에게 문의

### 문제 2: "Credential 오류" 또는 "401 Unauthorized"

**원인:** API Key 또는 Secret 오류

**해결:**
1. SolAPI Credential 설정 확인
2. API Key 및 Secret 재확인
3. SolAPI 콘솔에서 API Key 상태 확인
4. Credential 재생성

### 문제 3: "템플릿을 찾을 수 없음" 오류

**원인:** 템플릿 ID 오류 또는 템플릿 미승인

**해결:**
1. 템플릿 ID 확인 (환경 변수)
2. 템플릿 승인 상태 확인
3. 카카오 비즈니스에서 템플릿 승인 완료 확인
4. SolAPI 콘솔에서 템플릿 상태 확인

### 문제 4: "전화번호 형식 오류"

**원인:** 전화번호 포맷 오류

**해결:**
1. 전화번호 포맷 변환 노드 확인
2. 하이픈 제거 확인
3. 전화번호 유효성 검사 확인
4. SolAPI 요구 형식 확인

---

## 💡 SolAPI 추가 기능

### 네이버 메시지 발송

SolAPI는 네이버 메시지도 발송할 수 있습니다:

1. **Operation**: `Send NaverTalk` 선택
2. 네이버 메시지 템플릿 등록
3. 동일한 방식으로 발송

### SMS 발송

일반 SMS도 발송 가능:

1. **Operation**: `Send SMS` 선택
2. 메시지 내용 직접 입력
3. 전화번호로 발송

---

## 🎯 최종 워크플로우 구조

```
[Gmail Trigger]
  ↓
[IF: 예약 확정/취소 구분]
  ├─ True (확정)
  │   ↓
  │   [Code: 확정 이메일 파싱]
  │   ↓
  │   [Set: 데이터 정리]
  │   ↓
  │   [Code: 고유 링크 생성]
  │   ↓
  │   [Code: 전화번호 포맷 변환]
  │   ↓
  │   [Code: 템플릿 변수 생성] (선택)
  │   ↓
  │   [SolAPI: 카카오 알림톡 발송]
  │
  └─ False (취소)
      ↓
      [Code: 취소 이메일 파싱]
      ↓
      [Set: 취소 데이터 정리]
      ↓
      [관리자 알림] (선택사항)
```

---

## 📊 SolAPI vs 다른 솔루션 비교

| 항목 | SolAPI | 비즈톡 | 직접 API |
|------|--------|--------|----------|
| **n8n 노드** | 공식 노드 ✅ | HTTP Request | HTTP Request |
| **설정 복잡도** | 간단 ✅ | 복잡 | 복잡 |
| **가입 조건** | 상대적으로 간단 ✅ | 까다로움 | 까다로움 |
| **템플릿 관리** | SolAPI 콘솔 ✅ | 카카오 비즈니스 | 카카오 비즈니스 |
| **비용** | 건당 20-30원 | 건당 20-30원 | 건당 20-30원 |
| **다중 서비스** | 알림톡/SMS/네이버 통합 ✅ | 알림톡만 | 알림톡만 |

---

## 🎯 최종 권장사항

### SolAPI 사용 (최종 권장)

**이유:**
1. ✅ n8n 공식 노드 지원으로 설정 간편
2. ✅ 가입 조건 상대적으로 간단
3. ✅ 카카오 알림톡, 네이버 메시지, SMS 통합 관리
4. ✅ 템플릿 관리 용이
5. ✅ 비즈톡보다 가입 조건 덜 까다로움

**다음 단계:**
1. SolAPI 가입 및 서비스 활성화
2. 카카오 비즈니스 채널 생성 및 인증
3. SolAPI에서 알림톡 템플릿 등록 및 승인
4. n8n SolAPI 노드 설정
5. 테스트 실행

---

**문서 버전**: 1.0  
**최종 업데이트**: 2024-01-15
